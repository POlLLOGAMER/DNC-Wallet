package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Immersive UI Palette
val SpaceBlack = Color(0xFF0B0D11)          // Dark obsidian background from spec
val SlateBackground = Color(0xFF0F1319)     // Slightly lighter ambient space tint
val CardSlate = Color(0xFF161A24)           // Sleek semi-glass base
val BorderSlate = Color(0x1AFFFFFF)         // Subtle 10% translucent white border line

val TechCyan = Color(0xFF22D3EE)            // Vibrant neon cyan accent from spec
val AlertAmber = Color(0xFFFB923C)          // Cozy warning amber
val DncGreen = Color(0xFF34D399)            // Emerald green for nodes and assets
val SubtleText = Color(0xFF94A3B8)          // Slate-400 descriptive tone
val TextLight = Color(0xFFF1F5F9)           // Slate-100 high readability contrast

val TechCyanDim = Color(0x2622D3EE)         // 15% Cyan glow fill
val DncGreenDim = Color(0x1A34D399)         // 10% Green success fill

